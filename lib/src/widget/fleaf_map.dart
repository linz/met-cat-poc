import 'package:flutter/material.dart';

class FleafMap extends StatelessWidget {
  const FleafMap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Text("Map");
}
